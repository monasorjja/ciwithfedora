name: Python Tests 

on:
  push:
    branches: [ dev ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Set up Python

      - uses: fedora-python/tox-github-action
        with:
          tox_env: py312

